services:
  mongo:
    image: mongo:7
    container_name: pizzeria-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: pizzeria
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
    driver: local
